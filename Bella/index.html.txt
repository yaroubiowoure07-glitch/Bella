<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <title>Pour Bella❤️</title>
  <style>
    body {
      background: url("bella.jpg") no-repeat center center fixed;
      background-size: cover;
      overflow: hidden;
      margin: 0;
      padding: 0;
      font-family: 'Georgia', serif;
      color: #fff;
      text-align: center;
      min-height: 100vh;
      position: relative;
    }

    /* Voile sombre pour rendre le texte plus lisible */
    body::before {
      content: "";
      position: absolute;
      top: 0; left: 0;
      width: 100%; height: 100%;
      background: rgba(0,0,0,0.4); /* voile sombre */
      z-index: 0;
    }

    .contenu {
      position: relative;
      z-index: 1;
      padding: 20px;
    }

    h1 {
      font-size: 2.5em;
      margin-bottom: 0.5em;
      animation: battement 1.5s infinite;
    }

    .coeur-battant {
      font-size: 1.8em;
      margin: 10px 0;
      animation: battementCoeur 1.5s infinite;
      color: #fff;
      text-shadow: 0 0 10px #fff;
    }

    p {
      font-size: 1.3em;
      line-height: 1.6em;
      opacity: 0;
      animation: apparitionLumineuse 3s ease-in-out forwards;
      animation-delay: 1s;
    }

    @keyframes battement {
      0%, 100% { transform: scale(1); }
      50% { transform: scale(1.2); }
    }

    @keyframes battementCoeur {
      0%, 100% { transform: scale(1); opacity: 1; }
      25% { transform: scale(1.2); opacity: 0.9; }
      50% { transform: scale(1); opacity: 1; }
      75% { transform: scale(1.2); opacity: 0.9; }
    }

    @keyframes apparitionLumineuse {
      0% { opacity: 0; text-shadow: none; }
      100% { opacity: 1; text-shadow: 0 0 20px #fff; }
    }

    .boutons {
      margin-top: 30px;
      z-index: 2;
    }

    button {
      font-size: 1.2em;
      padding: 10px 20px;
      margin: 10px;
      border: none;
      border-radius: 10px;
      background-color: #fff;
      color: #ee9ca7;
      cursor: pointer;
      transition: transform 0.3s;
    }

    button:hover {
      transform: scale(1.1);
    }

    .coeur-explosion, .coeur-brise {
      position: absolute;
      font-size: 24px;
      pointer-events: none;
      z-index: 1000;
    }

    .message-oui, .message-non {
      position: fixed;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      font-size: 2em;
      padding: 20px 40px;
      border-radius: 20px;
      z-index: 2000;
      animation: fadeInOut 120s forwards;
      color: #000; /* texte en noir */
    }

    .message-oui {
      background: rgba(255, 255, 255, 0.8);
    }

    .message-non {
      background: rgba(255, 0, 0, 0.2);
    }

    @keyframes fadeInOut {
      0% { opacity: 0; }
      10% { opacity: 1; }
      90% { opacity: 1; }
      100% { opacity: 0; }
    }
  </style>
</head>
<body>
  <div class="contenu">
    <h1>Pour Bella ❤️</h1>
    <div class="coeur-battant">Je t’aime Bella 💗</div>
    <div class="coeur-battant">Je t’aime Bella 💗</div>
    <div class="coeur-battant">Je t’aime Bella 💗</div>
    <p>
      Bella… La première fois que nos regards se sont croisés, j’ai senti le monde ralentir.  
      J’ai longtemps hésité à te parler, mais aujourd’hui je n’ai plus peur : je t’aime, Bella ❤️
    </p>
    <p>
      Alors je te le demande : veux-tu être ma petite amie ? 💖
    </p>
    <div class="boutons">
      <button onclick="reponseOui()">Oui 💖</button>
      <button onclick="reponseNon()">Non 💔</button>
    </div>
  </div>

  <script>
    // Réponse Oui
    function reponseOui() {
      for (let i = 0; i < 50; i++) {
        const coeur = document.createElement("div");
        coeur.className = "coeur-explosion";
        coeur.innerText = "💖";
        coeur.style.left = Math.random() * window.innerWidth + "px";
        coeur.style.top = Math.random() * window.innerHeight + "px";
        coeur.style.animation = "pop 120s linear";
        document.body.appendChild(coeur);
        setTimeout(() => coeur.remove(), 120000);
      }

      const msg = document.createElement("div");
      msg.className = "message-oui";
      msg.innerText = "💖 Mon cœur est à toi, Bella, prends-en bien soin, comble-le d'amour et de joie ❤️";
      document.body.appendChild(msg);
      setTimeout(() => msg.remove(), 120000);
    }

    // Réponse Non
    function reponseNon() {
      for (let i = 0; i < 30; i++) {
        const coeur = document.createElement("div");
        coeur.className = "coeur-brise";
        coeur.innerText = "💔";
        coeur.style.left = Math.random() * window.innerWidth + "px";
        coeur.style.animation = "tomberEtBriser 120s linear";
        document.body.appendChild(coeur);
        setTimeout(() => coeur.remove(), 120000);
      }

      const msg = document.createElement("div");
      msg.className = "message-non";
      msg.innerText = "💔 Ton amour est beau même s'il n'est pas réciproque 💔";
      document.body.appendChild(msg);
      setTimeout(() => msg.remove(), 120000);
    }
  </script>
</body>
</html>
